import socket
import sys
import time
from multiprocessing import Process

#while true:
#	time.sleep(2)

	#if : #get messsage 
		# send message to gfd
p = Process(target = f ,args('bob',))
p.start()
p.join()

    
